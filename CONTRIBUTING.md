# 共通の開発手順

このアカウントのリポジトリでコードを変更する際は、各リポジトリ固有の `AGENTS.md`、README、運用手順も確認してください。固有の安全上の制約は維持します。

1. 対象リポジトリの構造、仕様、依存関係と既存Issueを確認する。
2. **コード変更前に必ずIssueを作成または更新**し、目的・現状・作業計画・変更予定ファイル・変更しないファイル・完成条件・テスト項目・作業ブランチを記録する。Issueへの記録が済むまで実装を始めない。
3. `main` またはリポジトリの既定ブランチから作業ブランチを作る。既定ブランチへ直接pushしない。
4. 原因と影響範囲を確認してから変更し、意味の分かる小さな単位でcommitする。検証結果をIssueへ追記する。
5. 作業終了または中断前に、導入済みなら `TASKS.md` と `HANDOVER.md` を更新する。未導入の場合はIssueに完了済み・未完了・次の手順を残す。
6. PRを作成し、Issue、変更内容、確認結果、残るリスクを記載する。レビュー後に既定ブランチへ統合する。

引き継ぐ人はIssue、ブランチ、commit履歴、差分、`HANDOVER.md`、`TASKS.md`、`PROJECT.md`、`DECISIONS.md`、`AGENTS.md` を確認し、未完了箇所から再開してください。

個人情報、APIキー、トークン、認証情報はIssue・PR・commit・公開文書へ記録しないでください。詳細は [development-standard](https://github.com/fululastock-siz/development-standard) を参照してください。
